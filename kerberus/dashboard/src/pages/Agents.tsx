import SectionTitle from '../components/SectionTitle'
export default function Agents() {
  return (
    <div className="page">
      <SectionTitle>Agentes</SectionTitle>
      <p>
        Agentes gerenciados pelo DeepSeek Harness (porta 3080).<br />
        Integração via API será disponibilizada na V2.
      </p>
    </div>
  )
}
