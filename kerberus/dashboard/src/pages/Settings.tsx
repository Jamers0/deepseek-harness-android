import SectionTitle from '../components/SectionTitle'

export default function Settings() {
  return (
    <div className="page">
      <SectionTitle>Configurações</SectionTitle>
      <p>
        Tema escuro padrão. Preferências do dashboard serão persistidas na V2.
      </p>
    </div>
  )
}
