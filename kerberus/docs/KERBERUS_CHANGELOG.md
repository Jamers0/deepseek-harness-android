# Kerberus Changelog

## 2026-09-02 — API v1, segurança mínima e rotas relativas
- Mantida a API real do S21 como base.
- Adicionados aliases `/api/v1/*` e `/api/docs`.
- Adicionada observação read-only do File Mesh com estado honesto `partial`.
- POST administrativo `/tasks` exige Bearer token configurado.
- Dashboard usa API relativa/proxy Vite.
- Bootstrap de tema antes do primeiro paint.
- Password do code-server removida da interface.
- Validação real pós-restart concluída no S21.
- Motorola permanece bloqueado e não foi classificado como funcional.
